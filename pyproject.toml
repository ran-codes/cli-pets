[project]
name = "cli-pets"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "rl627", email = "rl627@drexel.edu" }
]
requires-python = ">=3.12"
dependencies = [
    "rich>=14.2.0",
]

[build-system]
requires = ["uv_build>=0.8.22,<0.9.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "mkdocs-material>=9.6.22",
    "mkdocstrings[python]>=0.30.1",
    "python-dotenv>=1.1.1",
]
